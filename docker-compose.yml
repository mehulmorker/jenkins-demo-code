version: '3.8'

services:
  app:
    image: curiouslion/node-jenkins-demo:latest
    ports:
      - "8000:8000"
    environment:
      - NODE_ENV=production
      - PORT=8000
    restart: unless-stopped
